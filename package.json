{
  "name": "json-immutable-ex",
  "version": "0.3.2",
  "main": "lib/index",
  "scripts": {
    "prepublish": "babel src/ -d lib/",
    "test": "npm run prepublish && ava"
  },
  "peerDependencies": {
    "immutable": "3.x.x"
  },
  "dependencies": {},
  "devDependencies": {
    "ava": "0.16.x",
    "babel-cli": "6.14.x",
    "babel-preset-es2015": "6.14.x",
    "debug": "^2.2.0",
    "immutable": "3.8.x"
  }
}
